edge-tts
faster-whisper
requests
pillow
opencv-python-headless
flask
python-dotenv

# Auth, database, storage, deployment (multi-user web app)
flask-login
flask-wtf
flask-limiter
sqlalchemy
alembic
psycopg2-binary
boto3
gunicorn

# Tests only (tests/) — not a separate requirements-dev.txt, this project doesn't split
# dev/runtime deps anywhere else either.
pytest
