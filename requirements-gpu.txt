# Optional — only needed for pipeline/illustrate.py's GPU-based illustration generation
# (illustrated visual style, when a CUDA GPU is actually present — see
# Claude outputs/OPEN_ISSUES.md #61 and illustrate.py's own module docstring for why
# this is a separate file rather than folded into requirements.txt: these are large
# (~2.5GB+) downloads that a non-GPU deployment has no use for, and the code itself
# already degrades gracefully (torch import wrapped in try/except) if this file was
# never installed at all — an absent GPU stack is a normal, expected state, not an
# error. Installed by default in the Dockerfile via the WITH_GPU_ILLUSTRATION build arg
# (see Dockerfile), which docker-compose.yml sets for this specific, confirmed-GPU host.
#
# torch itself is NOT listed here — the Dockerfile installs it separately, first, from
# PyTorch's own CUDA-specific index (a plain PyPI `torch` would be the CPU-only build,
# silently defeating the entire point of this file). cu121 matches the driver/CUDA
# version confirmed present on this deployment host (NVIDIA driver 561.09, CUDA 12.6 —
# cu121 wheels are forward-compatible with newer drivers).
diffusers
transformers
accelerate
safetensors
